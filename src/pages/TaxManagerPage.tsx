
import { Layout } from '@/components/Layout';
import { TaxManager } from '@/components/TaxManager';

const TaxManagerPage = () => {
  return (
    <Layout>
      <TaxManager />
    </Layout>
  );
};

export default TaxManagerPage;
